<!DOCTYPE html>
<html>
<head>
  <title>Social Media Fingerprint</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/foundation/6.2.3/foundation.min.css">
  <link href='https://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <script src="https://cdn.jsdelivr.net/foundation/6.2.3/foundation.min.js"></script>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class='top-bar tip-top-styling'>
    <% if current_user %>
    <div class='top-bar-title app-name'>
      Social Media Fingerprint
    </div>
    <div class='top-bar-right'>
      <ul id='top-menu' class='menu'>
        <% if current_page?(images_path) %>
        <li>
          <%= button_to 'About', about_index_path, :method => :get, class: 'input-button-style', id: 'top-btn-2' %>
        </li>
        <li>
          <%= button_to 'Create New Fingerprint', new_image_path, :method => :get, class: 'input-button-style', id: 'top-btn-1' %>
        </li>
        <% elsif current_page?(new_image_path)%>
        <li>
          <%= button_to 'About', about_index_path, :method => :get, class: 'input-button-style', id: 'top-btn-2' %>
        </li>
        <% end %>
        <li>
          <%= button_to 'Sign Out', destroy_user_session_path, :method => :delete, class: 'input-button-style', id: 'top-btn-3' %>
        </li>
      </ul>
      <% end %>
    </div>
  </div>
  <%= yield %>
</body>
</html>
